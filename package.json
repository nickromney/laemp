{
  "name": "laemp-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for the laemp stack using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:slicer:smoke": "playwright test tests/e2e/slicer-smoke.spec.ts",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report"
  },
  "keywords": [
    "moodle",
    "testing",
    "playwright",
    "e2e"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.6.1"
  }
}
